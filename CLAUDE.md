# CLAUDE.md

This repository uses agent-agnostic instructions in `AGENTS.md`.

Claude Code should read `AGENTS.md` first, then follow the scoped `AGENTS.md` files under `backend/` or `frontend/` when working in those subprojects.
